int add(n):
    r = n + 1
    return r

void main():
    global x
    x = 2
    y = 4
    while x < y:
        x = add(1)
        y += 1

x = 1

print(x)


####


redeclaração -> qualquer sitio sem ser expression
Não-declaração -> expression
variaveis usadas mas não inicializadas -> Não tem value
variaveis declaradas e nunca mencionadas -> Tem value mas nunca são redeclaradas ou usadas na expression
