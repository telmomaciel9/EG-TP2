int add(n):
    r = n + 1
    return r

void main():
    global x
    x = 2
    y = 4
    while x < y:
        x = add(1)
        y += 1

x = 1

print(x)


####


DUVS/NOTAS:

No caso de termos:

                | NAME "=" expression -> assign_statementname
                | type NAME "=" expression -> assign_statementtype

Como as expressões podem ser quase infinitas, podiamos guardar isso no campo value do mapa e depois calcular o que da para calcular(se necessário) 
-> tinhamos que mudar a GIC para os simbolos nao serem ignorados ou então criar bues alias para saber que simbolo é (soma,div,mulp...)